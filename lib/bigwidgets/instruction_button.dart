import 'package:flutter/material.dart';
import 'package:rubickk/consts.dart';
import 'package:rubickk/widgets.dart';

class InstructionButton extends StatelessWidget {
  const InstructionButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // child: IconButton(
      //               onPressed: (){
      //                 Navigator.pushNamed(context, '/instruction');
      //               },
      //               splashRadius: 1,
      //                icon: Icon(Icons.help, color: AppColors.textColor,),
                     
      //                ),
    );
  }
}